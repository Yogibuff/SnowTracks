/* Front-end Javascript and JQuery */

// initially hides the search bar, will animate into place from left to center.
$('#searchField').hide().show('slow');

//Create a map object and specify the DOM element for display
